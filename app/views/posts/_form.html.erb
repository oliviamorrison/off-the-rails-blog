<%= image_tag "v3.png", class: "logo"%>
<%= form_with(model: post, local: true, class:"form") do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div class="label"><%= form.label :title%></div>
    <%= form.text_field :title, class: "title"%>
  </div>

  <div class="field">
    <div class="label"><%= form.label :content %></div>
    <%= form.text_area :content, class: "content" %>
  </div>

  <div class="button-container">
    <%= submit_tag "Create", class: "create-button"%>
  </div>
<% end %>
